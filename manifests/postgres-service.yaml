apiVersion: v1
kind: Service

metadata:
  name: geoquiz-postgres-service

spec:
  selector:
    app: geoquiz-postgres

  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432

  type: ClusterIP
